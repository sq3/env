## run as service from the Automator container "Run AppleScript"
## set keyboard shortcut to something you want (e.g. Ctrl + Alt + CMD + .)

tell application "iTerm"
	activate
	set t to (make new terminal)
	tell t
		launch session "Default Session"
	end tell
end tell
